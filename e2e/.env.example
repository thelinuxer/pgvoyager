# PostgreSQL Test Database Configuration
# This database will be used for E2E tests to connect to
TEST_PG_HOST=localhost
TEST_PG_PORT=5432
TEST_PG_DATABASE=pgvoyager_test
TEST_PG_USER=postgres
TEST_PG_PASSWORD=postgres

# Application URL
BASE_URL=http://localhost:5137

# PgVoyager SQLite database path (for cleanup between tests)
# Leave empty to use default: ~/.config/pgvoyager/pgvoyager.db
PGVOYAGER_DB_PATH=

# CI Mode (set automatically in GitHub Actions)
CI=false
